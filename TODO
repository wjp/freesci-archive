TODO for the next version:

- Fix the remaining bugs:
- Implement the "missing half" of the parser (Said())


TODO for 0.3.x:

- Re-structure the graphics subsystem for performance
- Implement sound servers that actually make some sound
- Merge Magnus' Python class browser into the main tree
